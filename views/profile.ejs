<% include partials/header %>

<div class="container">

    <div class="page-header text-center">
        <h1><i class="mdi mdi-anchor"></i> Profile Page</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-12">
            <div class="well">
            <a href="">Link Facebook Acount</a>
                <h3><i class="mdi mdi-account"></i> User Information</h3>

                    <p>
                        <strong>id</strong>: <%= user.id %><br>
                        <strong>email</strong>: <%= user.email %><br>
                        <strong>password</strong>: <%= user.password %><br>
                        <strong>profileImg</strong>: <%= user.profileImg %><br>
                        <strong>gamerTag</strong>: <%= user.gamerTag %><br>
                        <strong>facebook_id</strong>: <%= user.facebook_id %><br>
                        <strong>facebook_token</strong>: <%= user.facebook_token %><br>
                        <strong>google_token</strong>: <%= user.google_token %><br>
                        <strong>google_id</strong>: <%= user.google_id %><br>
                    </p>

            </div>
        </div>

    </div>

</div>

<% include partials/footer %>

